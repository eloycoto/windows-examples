# c551c749-ca14-401b-be0d-c8ca1464706f.myprj

Ansible project generated by x2a-convertor.

## Roles


### system_baseline


Establishes a baseline system configuration including timezone settings, user accounts, Windows features, firewall rules, and IE security settings


**Target platforms:** Windows


Run this role:

```bash
ansible-playbook playbooks/run_system_baseline.yml -i inventory/hosts.yml
```


**Default variables** (override in inventory or extra vars):

| Variable | Default |
|----------|---------|

| `timezone` | `Eastern Standard Time` |

| `demo_username` | `demo` |

| `demo_fullname` | `x2ansible Demo User` |

| `demo_description` | `Demo account created by system_baseline role` |

| `demo_password_never_expires` | `True` |

| `demo_password_change_not_allowed` | `True` |

| `firewall_rules` | `[{'name': 'X2A-Allow-HTTP', 'display_name': 'Allow HTTP Inbound', 'direction': 'inbound', 'protocol': 'tcp', 'local_port': '80', 'action': 'allow', 'enabled': True, 'profiles': 'domain,private,public'}, {'name': 'X2A-Allow-HTTPS', 'display_name': 'Allow HTTPS Inbound', 'direction': 'inbound', 'protocol': 'tcp', 'local_port': '443', 'action': 'allow', 'enabled': True, 'profiles': 'domain,private,public'}, {'name': 'X2A-Allow-RDP', 'display_name': 'Allow RDP Inbound', 'direction': 'inbound', 'protocol': 'tcp', 'local_port': '3389', 'action': 'allow', 'enabled': True, 'profiles': 'domain,private,public'}]` |




## Prerequisites

### Collections

Install required Ansible collections:

```bash
ansible-galaxy collection install -r collections/requirements.yml
```


## Inventory

Edit `inventory/hosts.yml` to define your target hosts before running any playbook.

## Quick start

```bash
# Install required collections
ansible-galaxy collection install -r collections/requirements.yml

# Edit inventory
vi inventory/hosts.yml

# Run a playbook
ansible-playbook playbooks/run_<role_name>.yml -i inventory/hosts.yml
```